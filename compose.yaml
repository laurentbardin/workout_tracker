name: workout_tracker

services:
  app:
    image: registry.gitlab.com/laurentbardin/workout_tracker
    build:
      context: ./
      dockerfile: ./contrib/docker/Dockerfile
      target: backend
    ports:
      - "8000:8000"
    volumes:
      - ./:/home/workout_tracker/
