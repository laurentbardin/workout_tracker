In no particular order:

* Allow for easy exercise swap during a workout
* Add a visual schedule editor (drag & drop)
* Display estimated time when starting a workout (mean of 5 previous
  instances?)
* Better HTMX integration:
    * Custom HtmxResponse (akin to HttpRespone) to define a list of template
      partials to render and return
    * Custom Views that only accept HTMX requests ('HX-Request' header)
